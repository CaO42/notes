### 环境光

即使在黑暗的情况下，世界上通常也仍然有一些光亮（月亮、远处的光），所以物体几乎永远不会是完全黑暗的。为了模拟这个，我们会使用一个环境光照常量，它永远会给物体一些颜色。

```c++
void main()
{
    float ambientStrength = 0.1;
    vec3 ambient = ambientStrength * lightColor;
    vec3 result = ambient * objectColor;
    FragColor = vec4(result, 1.0);
}
```

### 漫反射

模拟光源对物体的方向性影响(Directional Impact)。它是风氏光照模型中视觉上最显著的分量。物体的某一部分越是正对着光源，它就会越亮。

需要：法向量，光源位置

```c++
    //diffuse
    vec3 norm = normalize(Normal);
    vec3 lightDir = normalize(lightPos - FragPos);
    float diff = max(dot(norm, lightDir), 0.0);
    vec3 diffuse = diff * lightColor;
```

### 镜面反射

模拟有光泽物体上面出现的亮点。镜面光照的颜色相比于物体的颜色会更倾向于光的颜色。

```c++
    float specularStrength = 0.5;
    float shininess = 32.0;
    vec3 viewDir = normalize(-FragPos);//在观察空间中 viewPos固定为(0,0,0)
    vec3 reflectDir = reflect(-lightDir, norm);//reflect(I, N) = I - 2.0 * dot(N, I) * N 平行四边形原理
    float spec = pow(max(dot(viewDir, reflectDir), 0.0), shininess);
		//镜面反射强度取决于 反射向量与视角向量的夹角大小
    vec3 specular = specularStrength * spec * lightColor;
```

### 材质

每个物体会对光产生不同的反应，

```c++
struct Material {
    vec3 ambient;
    vec3 diffuse;
    vec3 specular;
    float shininess;
}; 

uniform Material material;
```

### 光照贴图

引入**漫反射**和**镜面光**贴图(Map)。这允许我们对物体的漫反射分量（以及间接地对环境光分量，它们几乎总是一样的）和镜面光分量有着更精确的控制。

一个**纹理**。我们仅仅是对同样的原理使用了不同的名字：其实都是使用一张**覆盖物体的图像**，让我们能够逐片段索引其独立的颜色值。在光照场景中，它通常叫做一个**漫反射贴图**(Diffuse Map)（在PBR之前3D艺术家通常都这么叫它），它是一个表现了物体所有的漫反射颜色的纹理图像。



### 投光体

将光**投射**(Cast)到物体的光源叫做投光物(Light Caster)
